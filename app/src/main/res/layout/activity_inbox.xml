<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="@color/dark_background">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="56dp">

        <!-- Header Bar with Gradient (Standard) -->
        <LinearLayout
            android:id="@+id/header_standard"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:background="@drawable/bg_header_inbox"
            android:gravity="center_vertical"
            android:paddingStart="16dp"
            android:paddingEnd="8dp"
            android:orientation="horizontal"
            android:elevation="6dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="📬 Inbox"
                android:textColor="#FFFFFF"
                android:textSize="20sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btn_home"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="🏠"
                android:textColor="#FFFFFF"
                android:backgroundTint="@color/primary_deep_purple"
                android:textSize="16sp"
                android:textAllCaps="false"
                android:minWidth="40dp"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:layout_marginEnd="4dp" />

            <Button
                android:id="@+id/btn_view_spam"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="⚠ View Spam"
                android:textColor="#FFFFFF"
                android:backgroundTint="@color/btn_spam"
                android:textSize="12sp"
                android:textAllCaps="false"
                android:paddingStart="12dp"
                android:paddingEnd="12dp" />

        </LinearLayout>

        <!-- Selection Mode Toolbar -->
        <LinearLayout
            android:id="@+id/header_selection"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:background="@color/primary_deep_purple"
            android:visibility="gone"
            android:gravity="center_vertical"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:orientation="horizontal"
            android:elevation="6dp">

            <ImageButton
                android:id="@+id/btn_cancel_selection"
                android:layout_width="48sp"
                android:layout_height="48sp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_sync"
                android:rotation="45"
                android:tint="#FFFFFF" />

            <TextView
                android:id="@+id/text_selection_count"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="1 selected"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginStart="8dp" />

            <ImageButton
                android:id="@+id/btn_batch_archive"
                android:layout_width="48sp"
                android:layout_height="48sp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_fingerprint"
                android:contentDescription="Archive All"
                android:tint="#FFFFFF" />

            <ImageButton
                android:id="@+id/btn_batch_delete"
                android:layout_width="48sp"
                android:layout_height="48sp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_delete"
                android:contentDescription="Delete All"
                android:tint="#FF5252" />

        </LinearLayout>
    </FrameLayout>

    <!-- Search Bar -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp"
        app:cardBackgroundColor="@color/search_background">

        <EditText
            android:id="@+id/edit_search"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@android:color/transparent"
            android:drawableStart="@drawable/ic_search"
            android:drawablePadding="12dp"
            android:hint="🔍  Search messages..."
            android:textColor="@color/search_text"
            android:textColorHint="@color/search_hint"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:singleLine="true"
            android:textSize="14sp" />
    </androidx.cardview.widget.CardView>

    <!-- Empty state -->
    <TextView
        android:id="@+id/text_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="📭 No messages found"
        android:textSize="16sp"
        android:textColor="@color/text_secondary"
        android:visibility="gone" />

    <!-- Swipe to refresh list -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_inbox"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:clipToPadding="false" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</LinearLayout>
